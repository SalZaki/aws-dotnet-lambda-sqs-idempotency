<Project>

  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <!-- Every test project uses xunit, so the using belongs here rather than in a Usings.cs each. -->
  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

</Project>
